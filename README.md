# REMO - Remote Control Bot

## 🚀 Quick Start

### 1. Start Server
```bash
python main.py
```

Server akan jalan di `http://0.0.0.0:8443`

### 2. Setup Cloudflare Tunnel (Kamu)
Setup tunnel untuk expose server ke internet

### 3. Set Webhook
```bash
python set_webhook.py
```

### 4. Test Bot
Buka Telegram → kirim `/start`

---

## 📋 Commands

| Command | Fungsi |
|---------|--------|
| `/start`, `/help` | Welcome & help |
| `/status` | CPU, RAM, Battery, Disk |
| **Power** ||
| `/lock` | Lock screen |
| `/sleep` | Sleep mode |
| `/shutdown` | Shutdown (konfirmasi) |
| `/restart` | Restart (konfirmasi) |
| **Audio** ||
| `/volume [0-100]` | Get/Set volume |
| `/mute` | Mute |
| `/unmute` | Unmute |
| **Display** ||
| `/screenshot` | Screenshot |
| `/brightness [0-100]` | Get/Set brightness |

---

## 🔧 Files

- `main.py` - Webhook server
- `set_webhook.py` - Set webhook to Telegram
- `config.py` - Configuration
- `bot/handlers.py` - Command handlers
- `system/` - Power, audio, display, status modules

---

## 🔐 Security

- User ID whitelist: Set in `.env` file
- Webhook secret: Static (saved to `.webhook_secret`)
- Rate limit: 30 cmd/min

---

## 📝 Notes

**Webhook Path (Static):**  
`/webhook/<auto-generated-secret>`

**Full URL:**  
`https://your-domain.com/webhook/<secret>`

_Actual values are configured in your `.env` file_
